<IfModule mod_rewrite.c>
    RewriteEngine on

    # we need this to resolve ./scp/apps.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_URI} (.*/scp)
    RewriteRule ^(.*)$ %1/dispatcher.php/$1 [L]
</IfModule>
